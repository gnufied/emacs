(add-hook 'ruby-mode-hook
	  (lambda ()
	    (yas/minor-mode)))

(add-hook 'python-mode-hook
	  (lambda ()
	    (yas/minor-mode)))
(add-hook 'feature-mode-hook
	  (lambda ()
	    (yas/minor-mode)))


(add-hook 'c-mode-hook
	  (lambda ()
	    (yas/minor-mode)))

(add-hook 'c++-mode-hook
	  (lambda ()
	    (yas/minor-mode)))


